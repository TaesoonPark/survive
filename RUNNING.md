# Survive — 실행 및 조작 가이드

Phaser 4 기반 톱다운 생존 게임. 이 문서는 **서버/클라이언트 구동 방법**과 **게임 조작법**을 다룹니다.
설계 원칙과 개발 규칙은 [AGENTS.md](AGENTS.md)를 참고하세요.

문서에 적힌 키·플래그·스크립트는 모두 실제 코드에서 확인한 값입니다.

---

## 1. 최초 준비

```bash
npm install
```

Node 22 이상이 필요합니다. 패키지 간에 빌드 단계가 없으므로(소스 레벨 경로 별칭) `npm install` 이후
바로 실행할 수 있습니다.

---

## 2. 가장 빠른 시작 — 서버 + 클라이언트 동시 실행

```bash
npm run dev
```

서버(`--mode dedicated --save world01`)와 Vite 개발 서버를 한 번에 띄웁니다.
브라우저에서 **http://127.0.0.1:5173** 을 열고, 메뉴의 **JOIN** 을 누르면 됩니다.
주소 입력란의 기본값이 이미 `http://127.0.0.1:27500` 이라 그대로 접속됩니다.

개별로 띄우려면:

```bash
npm run dev:server
```

```bash
npm run dev:client
```

---

## 3. 서버 단독 실행

```bash
npm run start:server
```

옵션을 주려면 `--` 뒤에 붙입니다.

```bash
npm run start:server -- --port 27500 --save myworld --seed 1337 --maxPlayers 8
```

빌드된 단일 파일로 실행하려면:

```bash
npm run build:server && node apps/server/dist/server.cjs --help
```

### 주요 서버 옵션

전체 목록은 `npx tsx apps/server/src/main.ts --help` 로 확인할 수 있습니다.

| 분류     | 옵션                               | 설명                                                                                          |
| -------- | ---------------------------------- | --------------------------------------------------------------------------------------------- |
| 모드     | `--mode <singleplayer\|dedicated>` | 기본 `dedicated`. `singleplayer`는 `127.0.0.1` 바인드 + 1인 + 일시정지 허용 + PvP 해제를 함의 |
| 네트워크 | `--bind <host>`                    | 바인드할 인터페이스 (모드에 따라 기본값 결정)                                                 |
|          | `--port <n>`                       | 포트. 기본 `27500`. `0`이면 빈 포트 자동 선택                                                 |
|          | `--password <secret>`              | 접속 비밀번호                                                                                 |
|          | `--maxPlayers <n>`                 | 최대 인원                                                                                     |
|          | `--aoiRadius <px>`                 | 복제 반경. 기본 `2560`                                                                        |
|          | `--statusPort <n>`                 | `/health`, `/status` 제공 포트. `0`이면 비활성                                                |
| 월드     | `--save <name>`                    | 월드 폴더 이름. 기본 `world01`                                                                |
|          | `--saveDir <path>`                 | 월드 폴더 위치. 기본 `./saves`                                                                |
|          | `--backend <fs\|sqlite\|memory>`   | 저장 백엔드. 기본 `fs`                                                                        |
|          | `--reset`                          | 시작 전에 월드 삭제                                                                           |
|          | `--seed <n>`                       | 새 월드의 시드                                                                                |
|          | `--urbanization <0..1>`            | 도시 밀도                                                                                     |
|          | `--zombieDensity <n>`              | 좀비 인구 배수                                                                                |
| 규칙     | `--pvp` / `--no-pvp`               | 플레이어 간 피해                                                                              |
|          | `--pauseEmpty` / `--no-pauseEmpty` | 접속자가 없으면 시뮬레이션 정지                                                               |
|          | `--cheats`                         | 디버그 커맨드 활성화                                                                          |
|          | `--needRate <n>`                   | 배고픔/갈증/피로 진행 배수                                                                    |
|          | `--cropGrowthRate <n>`             | 작물 성장 배수                                                                                |
| 성능     | `--simHz <n>`                      | 시뮬레이션 레이트. 기본 `20`                                                                  |
|          | `--snapshotHz <n>`                 | 스냅샷 레이트. 기본 `10`                                                                      |

> **보안 주의**: `dedicated` 모드의 기본 바인드는 `0.0.0.0`(모든 인터페이스)입니다.
> 로컬에서만 쓸 거라면 `--bind 127.0.0.1` 을 명시하세요.
> `singleplayer` 모드는 명세 §13에 따라 항상 루프백만 바인드합니다.

---

## 4. 클라이언트

```bash
npm run dev:client          # 개발 서버 (http://127.0.0.1:5173)
npm run build:client        # 정적 빌드 → apps/client/dist
npm run preview --workspace @survive/client   # 빌드 결과 미리보기 (포트 4173)
```

### 접속 화면

- **Join a server** — 주소(기본 `http://127.0.0.1:27500`), 플레이어 이름(기본 `Survivor`),
  비밀번호(선택)를 넣고 **JOIN**.
- **Single player** — 로컬 서버를 직접 띄우는 기능이라 **데스크톱 빌드에서만** 동작합니다.
  브라우저에서는 위처럼 서버를 따로 실행해 접속하세요. 메뉴에도 같은 안내가 표시됩니다.

---

### 캐릭터는 무엇으로 구분되는가

**플레이어 이름**입니다. 서버가 이름에서 저장 키(`playerId`)를 만들고, 같은 키로
다시 접속하면 그 캐릭터를 그대로 이어받습니다.

- 이름이 다르면 **다른 캐릭터**입니다. 한글·중국어·키릴 문자 이름도 서로 구분됩니다.
- 저장 파일 이름으로 쓰이므로 키 자체는 ASCII만 담을 수 있습니다. 그래서 `홍길동`은
  `survivor-0tlwojf`처럼 **이름의 해시를 덧붙인** 키가 됩니다. 사람이 읽을 이름은
  `홍길동` 그대로 표시됩니다.
- 대소문자는 구분하지 않습니다 — `Alice`와 `ALICE`는 같은 캐릭터입니다.
- 같은 이름으로 두 명이 동시에 접속할 수는 없습니다 (`characterTaken`).

**시작 인벤토리 등 캐릭터 생성 설정을 바꿔도 이미 있는 캐릭터에는 적용되지 않습니다.**
저장된 캐릭터를 그대로 불러오기 때문입니다. 새로 받으려면 다른 이름으로 접속하거나,
한 번 죽어서 부활하거나(부활 지급품이 잃어버린 것을 채워 줍니다), 월드를 초기화하세요.

### 월드 초기화

로비의 **월드 초기화** 버튼을 쓰거나, 서버를 다시 띄우면서 `--reset`을 주면 됩니다.

```bash
npm run dev -- --reset
```

버튼은 두 번 눌러야 실행됩니다. 첫 번째는 무엇이 지워지는지 알리고, 다른 곳을 건드리면
해제됩니다. 서버를 실행 중인 컴퓨터에서만 동작합니다 — 요청이 온 주소가 루프백인지 보고
판단하므로, 원격 플레이어에게는 `서버를 실행 중인 컴퓨터에서만 초기화할 수 있습니다`로
거절됩니다. 접속 비밀번호로는 열리지 않습니다. 비밀번호는 _입장_ 자격이지 월드를 지울
자격이 아니고, 그걸 아는 모든 플레이어가 남의 월드를 날릴 수 있게 되기 때문입니다.

초기화하면 지형·건축물·떨어진 아이템과 **저장된 모든 캐릭터**가 사라지고, 접속해 있던
플레이어는 전부 끊깁니다. 월드 이름과 시드는 유지되므로 같은 지형에서 다시 시작합니다.

## 5. 데스크톱 앱 / 서버 런처 (Electron)

```bash
npm run dev:desktop         # 데스크톱 앱 개발 모드
npm run dev:launcher        # 서버 런처 개발 모드
```

배포 패키지 생성:

```bash
npm run build               # 클라이언트 + 서버 번들을 먼저 만들어야 함
npm run package --workspace @survive/desktop
npm run package --workspace @survive/server-launcher
```

`package`는 `prePackage` 훅에서 `scripts/stage.mjs`를 실행해 클라이언트/서버 번들을
`build/resources/{client,server}` 로 복사합니다. 번들이 없으면 그 자리에서 실패하며
무엇을 먼저 빌드해야 하는지 알려줍니다.

싱글플레이는 데스크톱 앱이 로컬 서버를 **127.0.0.1** 에 띄우고 일회용 토큰으로 접속하는 방식입니다.
싱글과 멀티가 같은 `GameServer`를 쓰기 때문에 게임 규칙이 갈라지지 않습니다.

> 이 환경에는 디스플레이가 없어 GUI를 실제로 띄워 확인하지는 못했습니다.
> 두 앱의 메인 프로세스는 부팅·창 보안 설정·리소스 경로까지 자동 테스트로 검증되어 있습니다
> (`tests/unit/electronBoot.test.ts`).

---

## 6. 언어 설정

기본은 브라우저 언어를 따르고, 한국어와 영어를 지원합니다. `?lang=` 으로 강제할 수
있습니다:

```
http://127.0.0.1:5173/?lang=ko
http://127.0.0.1:5173/?lang=en
```

- 지원하지 않는 값이면 영어로 떨어집니다.
- **게임 내 전환 스위치는 없습니다.** 콘텐츠 표는 부팅 때 한 번 만들어지고 일부
  패널은 임포트 시점에 라벨을 채우므로, 전환하려면 어차피 새로고침이 필요합니다 —
  그게 `?lang=` 이 하는 일입니다.
- 번역은 두 파일입니다. 콘텐츠 이름·설명은
  `packages/game-data/src/strings/ko.ts`(387개 항목), 인터페이스 문구는
  `apps/client/src/ui/strings/ko.ts`(약 700개 키). 언어를 추가하려면 파일 하나와
  `Locale` 타입에 케이스 하나를 더하면 되고, 시뮬레이션·렌더러는 손댈 필요가 없습니다.
- 서버는 완성된 문장을 보내지 않습니다. 알림은 코드와 인자로 오고 문장은 클라이언트가
  만듭니다 — 어순과 조사가 언어마다 다르기 때문입니다.

## 7. 조작법

### 이동

| 키                          | 동작                           |
| --------------------------- | ------------------------------ |
| `W` `A` `S` `D` 또는 방향키 | 이동                           |
| `Shift`                     | 달리기 (스태미나 소모)         |
| `Ctrl` 또는 `C`             | 웅크리기 (느려지지만 조용해짐) |

발소리는 이동 거리 기준으로 발생하고 좀비를 끌어당깁니다. 웅크리면 소리가 줄고 달리면 커집니다.

### 전투 · 상호작용

| 입력                | 동작                                             |
| ------------------- | ------------------------------------------------ |
| **마우스 좌클릭**   | 공격 (근접 무기는 조준 방향 원호, 원거리는 발사) |
| **마우스 이동**     | 조준 방향 (이동 방향과 독립)                     |
| `Space`             | 방어 / 막기                                      |
| `E`                 | 상호작용 (문, 상자, 채집, 침대 등)               |
| `E` **누르고 있기** | 자원 채집을 쿨타임마다 자동 반복                 |
| `R`                 | 재장전                                           |
| `G`                 | 든 아이템 버리기                                 |

- **자원 채집은 `E`를 누르고 있으면 알아서 반복됩니다.** 나무 한 그루를 넘기려면
  열 번 넘게 때려야 하므로 연타할 필요가 없습니다. 재개 시점은 서버가 스냅샷으로
  보내주는 쿨타임(`useReadyTick`)을 따르므로, 클라이언트가 속도를 추측하지 않습니다.
  자원 노드에만 적용됩니다 — 문에 대고 누르고 있으면 여닫기를 반복할 테고, 떨어진
  아이템이라면 주변을 싹 주워버릴 것이기 때문입니다.
- **근접 공격을 하면 사거리와 부채꼴이 흰색 반투명으로 잠깐 표시됩니다.** 반경과 각도는
  서버가 실제 판정에 쓰는 무기의 `range`·`arcDegrees` 그대로라, 보이는 범위가 곧 맞는
  범위입니다. 창의 좁은 찌르기와 곤봉의 넓은 휘두르기가 실제 동작만큼 다르게 보입니다.
  맨손도 표시됩니다.
- 근접 공격은 **시야선**이 필요합니다. 벽 너머는 닿지 않습니다.
- 몸이 겹칠 정도로 붙은 대상은 조준 방향과 무관하게 맞습니다.
- 무기에는 내구도가 있고 0이 되면 손에서 사라집니다.

> **마우스 우클릭은 현재 아무 동작도 하지 않습니다.** 입력 검증은 통과하지만
> 시뮬레이션에서 이 버튼을 소비하는 코드가 없습니다 (`Button.Secondary`).
> 알려진 미구현 사항입니다.

### 화면 · 패널

| 키               | 패널                                                   |
| ---------------- | ------------------------------------------------------ |
| `I` 또는 `Tab`   | 인벤토리                                               |
| `Q`              | 제작                                                   |
| `B`              | 건축 (열어둔 채로 월드에 배치)                         |
| `H`              | 신체 상태 (부위별 부상 · 여기서 스킬 화면으로)         |
| `M`              | 지도                                                   |
| `Y` 또는 `Enter` | 채팅                                                   |
| `Z`              | 취침 (침대 근처에서)                                   |
| `T`              | 건축물 회전 (`Shift+T`는 역방향)                       |
| `F3`             | 디버그 오버레이                                        |
| `Esc`            | 건축 선택 해제 → 맨 위 패널 닫기 → 일시정지            |
| `1`~`8`          | 핫바 칸 선택 (인벤토리 열고 아이템 위에서 누르면 등록) |

상자를 열거나 사망하면 해당 패널이 서버 판단으로 자동 열립니다.

건축물은 조각을 고른 뒤 **월드를 좌클릭**해서 놓습니다. 미리보기가 놓일 칸을 보여주고,
초록색이면 놓일 것으로 예상되는 자리, 빨간색이면 서버가 거부할 것으로 예상되는 자리입니다
(최종 판단은 서버가 하고, 거부 이유는 머리 위 말풍선으로 옵니다). 하나 놓아도 선택은
유지되므로 연달아 놓을 수 있고, **`Esc`로 선택을 해제**합니다 — 인벤토리에서 모닥불처럼
설치물을 *사용*하면 건축 패널이 열리지 않은 채로 미리보기가 켜지므로, 패널의 `Clear`
버튼이 화면에 없습니다.

골조가 올라가는 동안 **작업자는 그 자리에 붙잡힙니다.** 예전에는 진행 바가 차는 동안
걸어서 떠날 수 있었는데, 그건 세계가 혼자 건물을 짓는 것처럼 읽혔습니다. 범위를 벗어나면
작업이 멈추므로, 붙잡힘은 실제로 짓고 있는 동안만 유지됩니다.

### 핫바

핫바 8칸은 **인벤토리 칸을 가리키는 포인터**입니다. 아이템을 복사하거나 옮기는 것이
아니라, 어느 칸을 그 키에 연결할지 정하는 것입니다.

**등록하는 두 가지 방법**

- 인벤토리에서 아이템을 **핫바 칸으로 끌어다 놓기**
- 인벤토리를 열고 아이템 위에 커서를 둔 채 **숫자키 누르기**

**해제**: 커서를 **빈 칸**에 두고 숫자키를 누르면 그 키의 연결이 풀립니다.

**선택**은 *교환*입니다. 등록된 키를 누르면 그 아이템이 주손으로 오고, 들고 있던 것은
그 칸으로 돌아갑니다. 비어 있는 키를 누르면 들고 있던 것을 집어넣습니다(맨손). 마우스
휠로도 칸을 순환합니다.

핫바 오른쪽 끝의 `맨손` 칸은 주손에 든 것을 **보여주는 표시**이고 선택 대상이 아닙니다.
`9`는 동작하지 않습니다 — 칸이 8개입니다.

**포커스 대상의 이름이 상호작용 링 위에 표시됩니다.** 링은 무언가 조준되었다는 것만
알려주고 그게 무엇인지는 말해주지 않아서, 덤불이 몰려 있으면 키를 눌러봐야 알 수
있었습니다. 아래의 마우스오버 툴팁과는 별개이고 동시에 뜹니다 — 커서가 가리키는 것과
플레이어가 붙어 선 것은 보통 다릅니다.

**마우스를 올리면 툴팁이 뜹니다.** 인벤토리·핫바·상자·신체 슬롯의 아이템은 이름, 상태,
피해량, 무게 등을 보여주고, 월드의 자원 노드·떨어진 아이템·건축물·좀비·동물도 커서를
올리면 설명이 나옵니다. 자원 노드 툴팁에는 **필요한 도구와 등급, 그리고 나오는 산출물**이
표시됩니다 — 등급이 모자란 도구로 때리면 아무 일도 일어나지 않는데 다른 피드백이 없어서
특히 중요합니다. 가진 도구가 없으면 그렇게 알려줍니다.

> **채팅은 아직 동작하지 않습니다.** 클라이언트는 `chat` 커맨드를 전송하지만 서버에 처리기가
> 없어 커맨드가 그대로 버려집니다 (프로토콜에 자리만 예약된 미구현 기능). 감사에서 확인된 사실입니다.

### 일시정지에 대해

`Esc` 일시정지는 **싱글플레이에서만** 실제로 시뮬레이션을 멈춥니다 (명세 §12).
전용 서버는 한 클라이언트의 요청으로 세계를 멈추지 않으며, 서버가 그 요청을 거부합니다.

---

## 8. 생존 기본

- **배고픔 / 갈증 / 피로**는 0(만족)에서 100(위험)으로 올라가고, 100에서 피해를 줍니다.
- **부상은 부위별**입니다. 출혈은 지혈하지 않으면 계속 피를 잃고, 골절은 부목이 필요합니다.
- **좀비에게 물리면 감염**될 수 있습니다 (`--infectionChance`로 조절).
- **체온**이 중요합니다. 봄 새벽은 영하로 떨어집니다.
- **작물은 서리에 즉사**합니다. 각 작물은 자기 계절 중반의 밤은 견디도록 되어 있지만,
  겨울과 맞닿은 계절의 시작·끝(어깨 구간)에는 서리로 죽을 수 있습니다.
  새 월드는 **봄 1일 아침 8시**에 시작하므로, 첫날 심은 작물은 그날 밤 서리를 맞을 수 있습니다.
- 세계는 시드에서 결정적으로 생성되고, 변경된 부분만 저장됩니다.

---

## 9. 검사 및 도구

```bash
npm run check               # format:check + lint + typecheck + 전체 테스트
npm run test:unit           # 단위
npm run test:integration    # 통합
npm run test:multiplayer    # 멀티플레이 (실제 소켓)
npm run test:gameplay       # Playwright (실제 Chromium + 실제 서버)
```

> 세 Vitest 프로젝트는 **순차 실행**하세요. 동시에 돌리면 CPU 경합으로 시간 기반 단정이
> 불규칙하게 실패합니다.

개발용 도구:

```bash
npx tsx tools/simulate.ts --ticks 10000 --players 4 --zombies 40 --generated
npx tsx tools/worldgen.ts --seed 1337 --cx 128 --cy 128 --chunks 3
npx tsx tools/profile.ts --players 4 --ticks 1500
```

- `simulate` — 서버·렌더러 없이 게임 규칙만 돌리고 결과를 요약. p95/p99와 예산 초과 횟수를 보고합니다.
- `worldgen` — 지형을 컬러 텍스트로 미리보기.
- `profile` — 실제 서버의 틱 비용 측정. `--seed`(단수)와 `--players`를 받습니다.

---

## 10. 문제 해결

**클라이언트가 검은 화면이다** — 숨겨진/백그라운드 탭에서는 `requestAnimationFrame`이 돌지 않아
장면이 갱신되지 않습니다. 창을 앞으로 가져오세요. 성능 문제가 아닙니다.

**`window.__survive` 의 메서드가 없다고 나온다** — Vite 미리보기 서버가 재사용되어 예전 번들을
서비스하는 중입니다. `npm run build --workspace @survive/client` 후 다시 시도하세요.

**패키징한 앱이 "Client bundle missing"을 띄운다** — `npm run build`로 클라이언트와 서버 번들을
먼저 만들어야 합니다. `package`가 그 둘을 리소스로 복사합니다.

**포트가 이미 사용 중** — `--port 0` 으로 빈 포트를 자동 선택하거나 기존 서버를 종료하세요.
