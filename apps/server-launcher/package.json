{
  "name": "@survive/server-launcher",
  "version": "0.1.0",
  "private": true,
  "description": "Electron GUI that configures and supervises a dedicated GameServer process. Never hosts game logic itself.",
  "main": "./dist/main.cjs",
  "scripts": {
    "dev": "node ./scripts/dev.mjs",
    "build": "node ./scripts/build.mjs",
    "package": "electron-forge package",
    "make": "electron-forge make"
  },
  "devDependencies": {
    "electron": "^43.4.1",
    "@electron-forge/cli": "^7.11.2",
    "@electron-forge/maker-zip": "^7.11.2",
    "esbuild": "^0.25.0"
  }
}
